# Active Task Execution & Tracking

> **Session Continuity Log**: This file tracks the active milestone, task breakdown, architectural decisions, and next steps across AI coding sessions.

---

## Current Goal / Active Milestone

**Milestone**: NIBS Banking Prototype MVP — Screen Implementation & State Switcher Fidelity
- **Objective**: Ensure comprehensive coverage of all customer & admin prototype screens according to the Screen Consolidation specification, with zero-drift styling, 5-state coverage, and strict Light/Dark mode validation.

---

## Checklist & Progress

- [x] **Repository Foundation & Setup**
  - [x] Next.js 15 + React 19 + Tailwind v4 + Base UI configuration
  - [x] Design token definition in `src/app/globals.css` with OKLCH variables
  - [x] Session store persistence with `useSessionHydrated` protection
  - [x] Dual-shell architecture separation (`(customer)` and `admin`)
  - [x] Persistent AI context files created in `.ai/` (`INTERFACE.md`, `LEARNINGS.md`, `EXECUTION.md`)

- [ ] **Core Customer Shell Screens**
  - [x] Overview / Dashboard (Liquidity, Quick Actions, FX, Insights)
  - [x] Accounts List & Account Detail views
  - [x] Cards List & Card Details (limits, controls, freeze/unfreeze)
  - [x] Send & Pay (Send Money, Pay Bills, Standing Orders, Hub) — Figma 498:3460
  - [ ] FX Rates & Currency Exchange Conversion
  - [ ] Statements, Documents & Compliance Audit log

- [ ] **Admin Portal Shell Screens**
  - [x] Fee Concessions Table & Management
  - [ ] User Access & Role Administration
  - [ ] Maker-Checker Approval Queue & Dialogs
  - [ ] Operations & System Audit Logs

- [ ] **Quality & Verification Checks**
  - [x] `npx tsc --noEmit` validation (zero type errors)
  - [x] `npm run build` verification (37 routes generated successfully)
  - [ ] Dual-theme visual check (Light and Dark mode fidelity)
  - [ ] Every screen has functional `StateSwitcher` with all 5 states

---

## Recent Decisions / Audit Log

| Date / Session | Decision / Change | Rationale |
|---|---|---|
| Initial | Strict Semantic Token Enforcment | Guarantees instant dark mode compatibility and zero visual drift. |
| Initial | Font Weight Cap (`font-weight: inherit`) | Typography uses size and letter-spacing for hierarchy instead of heavy bolding. |
| Initial | Floating-point Money Utility Module | Eliminates JavaScript floating-point rounding bugs across monetary aggregates. |
| Initial | Asynchronous Hydration Gate | Prevents hydration race condition in Zustand session store on route guards. |
| Initial | AI Context System Initialized | Standardized `.ai/INTERFACE.md`, `.ai/LEARNINGS.md`, and `.ai/EXECUTION.md` for consistent agent pair-programming. |
| 2026-08-26 | Figma design timeline authored — [`DESIGN-TIMELINE.md`](../DESIGN-TIMELINE.md) | Retail IA (7 hubs / 19 nodes) costed at ~263 frames; 14-week single-designer or 9-week two-designer track. Decisions D2, D3, D5 still open before W1. |
| 2026-08-26 | **D1 decided — Personal and Business are two separate views, segmented pre-login** | The customer picks Personal or Business *before* signing in; each door has its own sign-in and there is no in-session profile switcher. Dual-relationship customers sign out and re-enter. Drops `/profile-selection` and profile-conditional nav from the Personal view; adds a 9-frame Entry & segmentation block. Trade / Approvals / Administration leave this plan for a separate Business-view timeline. **Not yet implemented** — `src/lib/navigation.ts` still carries the old conditional model. |
| 2026-08-26 | **One-time-code authorisation before every issued transaction** | `railSteps()` appends a universal `authorise` step to every rail (immediate, scheduled and repeating alike), and standing instructions created from `/payments/standing` pass the same gate. Logic lives once in `useAuthorisation` with a shared `AuthorisePanel`; `OtpInput` is reused from the MFA screen. Unlike sign-in MFA a completed code does **not** auto-submit — a payment is not recoverable — and stepping back discards the code. Pause/cancel are deliberately NOT gated (no asymmetric friction on stopping a payment). Receipts now carry an "Authorised" row. |
| 2026-08-26 | **D4 decided and built — one Send Money hub, one rail picker** | `/payments/new` and `UniversalTransferSheet` **deleted**. One machine (`src/components/payments/PaymentFlow.tsx`) now serves `/payments/send` (bank, ACH, wallet, proxy, group, mobile money, PAPSS) and `/payments/bills` (airtime, ECG, bills & merchants, Ghana.gov), matching the IA. Fee and arrival copy come from a single `RAIL_FACTS` table so the quote can't drift between picker, amount, review and receipt. Old deep links survive as `?source=` / `?duplicate=` / `?rail=`. PAPSS replaced the generic SWIFT wire. |
| 2026-08-30 | **Send & Pay Module Overhaul — Figma node 498:3460 Alignment** | Rebranded navigation to **Send & Pay**. Implemented 3-category hub layout (*Send*, *Pay*, *Scheduled*). Aligned Send Money and Pay Bill to 4-step wizard (*Recipient* with live lookup -> *Amount* with source card & *When?* control -> *Review* -> *Authorise* & *Receipt*). Rebuilt Standing Orders hub with cards and 4-step creation modal. |
| 2026-09-03 | **Beneficiaries Directory Standalone Tab & Dual-Journey Payment Groups** | Promoted Beneficiaries to a first-class Level-1 nav tab under Move Money (`/beneficiaries`) with full CRUD. Unified Payment Groups (Susu circles, family pools) into a shared Zustand store (`src/lib/groups-store.ts`). Implemented in-flow contextual group creation in `PaymentFlow.tsx` (`+ Create new group`) with immediate auto-selection, eliminating phantom lists and preventing flow abandonment. |
| 2026-09-11 | **Proxy & Group — chooser screens** | Proxy rail now opens on a chooser: *Transfer to a proxy ID*, plus manage-your-own — *Update* / *Deregister* (with a confirm), swapping to *Create a proxy ID* once none is registered. The customer's own proxy registration lives in a new persisted store `src/lib/proxy-store.ts`, edited via `ProxyIdModal.tsx` (phone / Ghana Card + linked account). Group rail opens on *Transfer to group* vs *Manage groups* (routes to `/beneficiaries?tab=groups`; page now reads the `tab` param), with a quick-pick strip of existing groups. Both gates use `proxyCategory` / `groupCategory` state, wire into the back button, and are bypassed when a saved recipient/group is picked. |
| 2026-09-11 | **Airtime & Data — self/other chooser before details** | Airtime and Data rails now open on a "Who is this for?" gate (mirrors the Mobile Wallet chooser): recent recipients strip + two cards — *My own number (Self)* and *Someone else*. Self prefills the registered number, detects the network (and first bundle for Data) and lands on a collapsed recipient summary, exactly like Send-to-Self; Other clears the fields and shows manual entry. One shared `topupCategory` state in `PaymentFlow.tsx`; back button returns to the chooser; picking a saved recipient bypasses it. |
| 2026-09-11 | **Onboarding Pass 1 — Guided persona tours (spotlight walk-throughs)** | Reworked the Persona & Flow switcher from teleport-and-drop into guided tours. New tour engine: `src/lib/tour-store.ts` (Zustand, non-persisted), `src/lib/tours.ts` (5 user-type tours as ordered `data-tour` steps), and `src/components/dev/TourOverlay.tsx` (rAF-tracked spotlight ring + dim + coach card; dim is `pointer-events:none` so the tester clicks the real control and both page and tour advance in lockstep; Back/Skip/Esc for control). Anchored `data-tour` on `/`, `/login`, `/get-started`, `/activate`, `/signup`, `/mfa`. Switcher now lists the 5 onboarding user types as guided walk-throughs + a "Jump straight in" section preserving the joint/mandate/mobile-sync personas. Fintech-UX: spotlight not takeover (in control), narrates *why* each step (no judgment). **Decisions honoured:** selfie stays in every flow this pass; new-device selfie, Wallet/Card split, and the primary-account picker are Pass 2. |
| 2026-09-22 | **Dashboard polish pass (`new-dashboard` branch) — no structural change** | Mobbin MCP unavailable (paid plan), so references were Monzo / Revolut / Mercury / Wise / Chase. Added: 30-day money in/out line under total balance (`cashFlowForProfile`); conditional *Needs attention* band (existing `AttentionBand`, outgoing failed payments only, amber not red, "see why and retry"); collapsed-state peeks (allocation bar on Accounts, `USD 11.55` on FX); time-of-day greeting; friendly dates in Recent activity. Fixed: *Pay again* had duplicated phantom payees → 8 real saved payees deep-linked to their rail with `?recipient=`; Top-Up pointed at Pay Bill → `rail=airtime`; FX mini used hardcoded rates that disagreed with `/fx-rates` → now `FX_RATES` + real publish time; dead promo prev/next arrows removed. Cards freeze is neutral (not destructive) with an *Undo* toast. Analytics defaults to 1m, labels the period, shows GHS, gains a top-3 legend that drives the same highlight as hover (touch/keyboard reachable), zero-bold fix. |
| 2026-09-22 | **Dashboard analytics wired to the ledger** | `SpendsRadialChart` no longer carries hardcoded `PERIOD_DATA`; it takes `byRange` from `spendByRangeForProfile()` (`src/lib/dashboard-insights.ts`), one `spendBreakdownForProfile(kind, days)` per pill (1w/1m/3m/6m/1y = 7/30/91/182/365 days), trailing from the latest ledger entry, debits with a category that didn't fail, top 5 + Other. `SPEND_RANGES`/`SpendRange` now live in insights. Empty periods draw a muted track with "No spending · past week". Note: the retail ledger only spans Jul–Aug 2026, so 3m/6m/1y currently read the same. |
| 2026-09-23 | **Analytics chart: stable segments across ranges** | Colour and position were assigned by per-range rank, so switching pills reshuffled segments. `spendByRangeForProfile` now uses the 1y top-5 + Other as one shared, ordered set for every range; the chart tweens each segment's start/end angle (420ms, snaps under reduced motion). Trade-off: a category outside the yearly top 5 shows as Other even in a short range. |
| 2026-09-23 | **Cards: request/replace wording, fees, Block rename** | Request flow: "Linked account" → **Source account**, "Fulfillment" → **Mode of delivery**; issuance fee now a figure per type (`ISSUANCE_FEE` in `RequestCardFlow.tsx`: Debit GHS 50, Prepaid GHS 30, Virtual GHS 10 — **placeholder values, confirm with product**), charged to source account; physical success copy "…has been submitted for production". Card detail: card type on the card face; the money line is a subtitle under the card name — "Balance GHS …" for prepaid/virtual, "Spends from <account> •• 1234" for debit — so every card screen keeps the same layout (balance on the card or in the right column stretched the card; original two-column sizing kept). **Replace card** keeps its warning dialog then opens `/cards/request?replace=<id>` on the details step with account, nickname, network, tier (base tier fallback), theme and delivery prefilled; balance carries over instead of new funding; authorising blocks the old card. "Freeze" → **Block** across dashboard, card detail, `VirtualCardModal` and `DashboardCardsWidget` (Lock icon). |
| 2026-09-23 | **Global language switch — every screen in EN / FR / ES / ZH, no cloud keys** | Kept the first-pass `t()` dictionary and added a DOM translator driven by a static phrase catalog (~2,850 rows × 3 languages, `src/lib/i18n/catalog/`), so all customer, onboarding, business and admin screens translate without rewriting 197 files. Lazy-loaded for non-English users only; hydration-gated; boot script prevents English flash; dates localised, money stays `GHS 1,234.56`. Customer data (names, merchants, banks, references) intentionally untranslated. `scripts/i18n-coverage.mts` lists copy missing from the catalog. **Translations are first-draft — a native-speaker review of money/legal wording is needed before real users.** |
| 2026-09-23 | **Accounts page rebuilt — accounts and sources of funds split (`feat/accounts-sources-split`)** | Follows the "Wallets as Plumbing" proposal. `/accounts` now has two sections: *Your accounts* (where money lives; default set on the account via the row menu; total across active accounts only; "Open a new account" → COOS portal, no "Add account") and *Add money from* (linked MoMo/cards, no balances, one-step Remove with Undo, "Link a card or wallet"). GCB customers never see a wallet; wallet customers (MoMo/card onboarding) see "GCB Wallet" as their one account plus "Open a GCB account", which previews the balance move and lands on the *just migrated* state. Linked sources and the default account live in `src/lib/accounts-store.ts`, shared with `LinkSourceAccountModal` (now "Add money", plus a `mode="link"` that links and closes). Dev Mode gained **grouped options** (`DevStateData.groups`, `DevStateMenuItems`): *Customer* (7 configurations in `src/lib/accounts-scenarios.ts`) × *Screen state* (populated / loading / accounts error / sources error). Search and `filtered-empty` removed from this page — 1–3 accounts don't need search. Business profile keeps accounts only (no sources section) — **confirm**. Dashboard still shows the old total-balance hero. |

| 2026-09-24 | **Forgot password rebuilt — mobile → selfie → new password → sign in** | `/forgot-password` drops email + OTP for: registered mobile number (`+233` prefix, digits only, capped at 9, grouped `24 123 4567` as typed, normalises pasted `+233…`/`0…`) → `SelfieCapture` (matched to the Ghana Card photo) → new password + confirm → "Password reset" with one *Back to sign in* button. Now uses `AuthLayout` with a 3-segment progress bar and a Back link on each step. The mobile step never reveals whether a profile exists. New shared `src/components/auth/NewPasswordFields.tsx` (show/hide, live `PASSWORD_RULES` checklist, mismatch hint, `newPasswordReady()`) is now used by **both** reset and `/activate`, which moves activation from its own 8-character checklist to the shared **12-character** rule (activation's `000000` demo error can no longer be reached). No demo failure state for a selfie mismatch yet. i18n catalog entries added for the new strings. |
| 2026-09-24 | **One phone field everywhere — `PhoneInput` + `src/lib/phone.ts`** | Every Ghana mobile field now uses `src/components/ui/phone-input.tsx`: fixed `+233` prefix, digits only, capped at 9, grouped `24 123 4567` as typed, numeric keypad, and it cleans up pasted or autofilled `+233…`, `233…` and `0…`. `onValueChange` always emits the local form `0241234567` (what `detectTelcoNetwork`/`resolveAccountName` already key off); `value` accepts any format, so seeds like `+233 24 412 3456` display correctly. Swapped in: forgot password, business sign-up (contact + signatories), beneficiaries (wallet + airtime), Airtime, Data, Mobile Wallet, Cardless Withdrawal, Standing Orders (×3), card delivery phone, Link MoMo, Proxy ID (phone type), Settings, Cheque book. Validity checks now require a complete number (`isCompleteGhanaMobile`) instead of `length >= 9`. Read-only rows use `displayGhanaMobile()`. **Not converted** (they take either a phone *or* an account/card number): group member entry in `CreateGroupFlow`/`EditGroupModal`, and the sandbox dashboard search. |
| 2026-09-24 | **Forgot password — selfie mismatch path** | A failed selfie lands on "We Couldn't Match Your Selfie" (likely causes, *Try again*, remaining tries shown). After 3 misses the flow stops offering retries: "Let's Finish This at a Branch" → *Find a branch* (GCB branch locator) / *Back to sign in*. Neutral icons, not red. Demo trigger: any mobile number ending in `0000` never matches. |
| 2026-09-24 | **Add money — choose the destination account** | `LinkSourceAccountModal` no longer locks funding to the account it was opened from. Both funding forms carry the Send & Pay "To Account" dropdown (`FromAccountSelector` with `label="To Account"`): *Transfer between accounts* is now From → To → Amount → Narration (each list excludes the other's pick), and *From linked wallet or card* is Payment method → To Account → Amount. The destination is preselected with the account Add money was opened from (or the default) and re-seeded each time the modal opens. The static destination card on the first screen was removed so the destination is chosen once, in the form. List = active GHS accounts (MoMo and local cards settle in cedis). The MoMo prompt names the destination. |
| 2026-09-24 | **My Expenses is per-account — global hub removed (`feat/account-expenses`)** | The *My Spends* button, `?tab=spends` and `MySpendsWidget` (hardcoded mock data + an "All accounts" selector) are gone from `/accounts`. Expenses are now reached only from Account Details: an `ExpensesGlance` row under the hero (30-day total, category bar, top 3) opens `/accounts/[id]/expenses`. That page never re-selects the account; one control — period pills (7 days / 30 days / 3 months / 12 months, in `?period=`, default 30d). Donut + category list come from the ledger via new `accountExpenses()` in `src/lib/insights.ts` (shares `rankSlices` with `spendByCategory`; "Other" carries `members`). Picking a category filters the account's own *Payments* list below; *Full statement* links out. Transfers between own accounts carry no category, so they never count as spend. Comparison vs the previous window is neutral copy ("GHS X less/more than the previous 30 days"), no red/green. **Cut:** Transaction/Channel view (it only ever had mock data — no channel on the ledger), account selector, "Click to expand" Other drill-in (members listed inline instead). **Note:** the chart reads the 24-month generated ledger while Payments lists the curated fixtures, so a category can show spend with few/no itemised rows — the filtered-empty state points to the statement. Breadcrumb label `expenses → My Expenses`; i18n entries added. |
| 2026-09-24 | **My Expenses page restyled from Figma 1867:4819 ("My Spends"), improved** | Kept the design's layout: *Category / Transaction type* tabs + "Last N days" calendar dropdown, large open donut (no panel) with "You’ve spent" + total centred, breakdown list beside it with square swatches, *Activity* below. **Transaction type is now real:** every ledger debit carries a `channel` (`SPEND_CHANNELS`: Card payments, Mobile money, Bank transfers, Bills & airtime, Cross-border, Bank fees) — generated entries rotate a per-category channel list (no extra PRNG draws, so every other chart's numbers are unchanged), curated ones map `paymentMethod` via `channelForPaymentMethod()`; `accountExpenses(…, view)`. **Improvements over the design:** scoped to one account (no account picker), Category is the default tab, breakdown rows are selectable and filter Activity + highlight the donut, neutral comparison under the donut, Activity lists only money out (the design showed "+ Transfer to Savings" credits, which aren't spending) and drops the redundant "Completed" badge, "View all" → this account's *Full statement*, semantic `--cat-*` colours instead of the gradient. |
| 2026-09-24 | **No balances on `/accounts`** | Account rows lose the amount + "Available" label and the *Your accounts* heading loses the Total. A balance now appears only on Account Details, where an eye button beside "Available balance" hides/shows it — it drives the same `AmountVisibilityProvider` state as the header eye, so there is one switch, not two. Kept: in-flight top-up amounts and the wallet→account migration preview on `/accounts` (money moving, not a balance). Dashboard balances untouched. |
| 2026-09-24 | **Account Details rebuilt — one centred column (mock review + current page combined)** | Reviewed a proposed tabbed layout (balance card + *Account Options / Activity / Request* tabs). Took its single calm balance card and inline eye; rejected the tabs (they hid Activity and the money actions, and mixed a menu, data and actions as peers). New order in `accounts/[id]/page.tsx` (max-w 720, centred): **Balance card** — grouped account number + copy, *Share details*, available balance + eye (same switch as the header eye), ledger balance only when it differs, **Add money** / **Pay** → **My Expenses** glance → **Activity** (last 5, *Full statement*) → **Manage**: Standing orders (live count + next run from `STANDING_INSTRUCTIONS`), Linked cards (one card → its page, where limits live; several → `/cards`), Statements & requests (expands in place: statement, cheque book, reference letter). **Cut:** Type/Currency/Status strip (title names type, balance names currency; non-Active status is a badge), the second "Account Details" card (→ *Share account details* dialog: holder, number, bank, SWIFT, per-row copy + *Copy all*), header *Standing Order* / *Request* buttons, the dead `/requests/card` link, the "Spending limits" row from the mock (no account-level limits exist — card limits live on the card). Raw hex surfaces replaced with tokens; back arrow uses the shared `BackLink`. `/accounts/[id]/expenses` centred to max-w 880 to match. |
| 2026-09-24 | **Linking a wallet or card now waits for the customer's approval** | **MoMo:** *Link wallet* / *Save & use wallet* no longer save straight away. They open a `link_momo_pending` screen in `LinkSourceAccountModal`: "Approve on your phone. We've sent a request to 024 123 4567. Approve it with your MTN MoMo PIN, then come back here to confirm." → *I've approved it* saves the wallet. *Didn't get it? Send again* works once; Back returns to the number form. **Card:** the Cardholder name field is gone. Card number is grouped as you type, expiry auto-inserts the `/`, and CVV takes digits only. The button stays disabled until all three are complete, with one line underneath: "Your bank will ask you to confirm it's you, then bring you back here." Submitting takes you to **`/card-verification`**, a stand-in for the card issuer's 3-D Secure page. It sits outside the customer shell and shows the card, "GCB Bank" as the merchant and "GHS 0.00 · card check only", plus a 6-digit code. Any code verifies and `000000` shows the error. You can also *Cancel and return to GCB*. `src/lib/card-link.ts` (a sessionStorage Zustand store) keeps the pending card (last 4, expiry and network only) across the round trip and saves the source on success. `useCardLinkReturn()` on `/accounts` and `/accounts/[id]` then shows a toast. When the card was added from Add money, the modal reopens on *From linked wallet or card* with the new card already selected (new props `initialScreen` / `initialSourceId`). A cancelled link shows "Card not linked" with *Try again*. **Not done:** i18n catalog rows for the new strings; no "prompt expired / declined" state for MoMo. |
| 2026-09-24 | **Account Details matches Figma 1896:15266; new Place a request flow** | `accounts/[id]/page.tsx` now follows the frame (max-w 560). **Balance card:** slate-tinted card (new `--account-card` token, light and dark) with a decorative dot map (`public/images/figma/account-card-pattern.svg`, downloaded from Figma). It shows the type and account number, the balance with its eye (same switch as the header), and Type / Currency / Status. **Buttons:** *Top up* (primary, opens the Add money modal) and *Share details* (outline). **Rows:** four rows on `--tile`: *My Expenses* → `/accounts/[id]/expenses`, *Standing orders* → `/payments/standing`, *Place a request* ("Statements, cheque books, bank letters") → `/accounts/[id]/requests`, and *Last 10 transactions*, which opens a dialog with a *Full statement* button. **Removed to match the design:** the inline Activity list, the Manage list, the Linked cards row, the ExpensesGlance numbers, the copy-number button (Share details covers it) and the ledger-balance line. Icons are Lucide stand-ins for the Material icons in Figma, per the Lucide-only rule. **Request flow** (`src/components/accounts/RequestFlow.tsx`) is shaped like Send & Pay: the page opens on "What would you like to request?" with three tiles (Bank statement / Cheque book / Bank letter). Cheque book is disabled on non-current accounts ("Only available on current accounts"). Each question appears only after the previous one is answered, then a review card with the fee, then PIN, then a receipt with a REQ reference. **Statement:** period (3 / 6 / 12 months or custom dates) → email (signed PDF, GHS 10) or collect at a branch (stamped copy, GHS 20) → branch. It also links to the existing self-serve statement download. **Cheque book:** 25 / 50 / 100 leaves (GHS 35 / 60 / 110) → collection branch; the printed name comes from the profile and isn't asked. **Letter:** purpose (confirmation / proof of funds / visa / other) → addressee (optional, default "To Whom It May Concern") → email (GHS 50) or branch (GHS 75) → branch. `?type=` skips the chooser. The account is never re-selected. **Fees and turnaround are placeholders; confirm with product.** The old `/requests/cheque-book` and `/requests/reference-letter` pages no longer have any links pointing to them. Breadcrumb `requests → Place a request`. |
| 2026-09-24 | **One shared `ActionTile`; system controls on Account Details and Place a request; My Spends is breakdown-only** | The Send & Pay hub tile ("To Bank") was copied inline in three places, so it's now `src/components/ui/action-tile.tsx`: tile fill, white icon chip, 16px label, optional description, chevron; it renders a link with `href`, otherwise a button (it can be disabled). The Send & Pay hub, the Account Details rows and the Place a request chooser all use it. The copies inside `PaymentFlow.tsx` haven't been moved over yet. Both pages now use `PageHeader`'s standard back arrow instead of the Figma circle. Place a request now uses the Send & Pay form controls: the 14px label with the h-13 select (payment-method style: name and description on the left, fee and ready time on the right), the matching text and date inputs, `ProceedButton`, the review cards and `PaymentSuccessScreen` (no action cards, no save-beneficiary; the reference goes in the message because that screen doesn't show receipt rows). **My Expenses → My Spends** on the tile, page title and breadcrumb (catalog row added). The Activity section and its category filter came off the page; picking a category still highlights the donut. Removed the unused `ExpensesGlance`, `SliceBar` and `expensesHref`. |
| 2026-09-24 | **Every navigation tile is now `ActionTile`; old request pages deleted** | `/requests/cheque-book` and `/requests/reference-letter` are gone; Place a request (`/accounts/[id]/requests`) replaces them. The 17 hand-copied tiles in `PaymentFlow.tsx` now use `ActionTile`: the bank, wallet, cardless and airtime/data choosers, the GCB Pay categories, and the proxy and group choosers, whose `*OptionCls` / `*IconCls` constants are removed. The one in `StandingOrderFlow.tsx` "Select a standing order" does too. The payments choosers keep their `p-4.5` padding via `className`. `ActionTile` gained a `leading` slot for chip content that isn't a plain icon: the "GCB" wordmark and the red Deregister bin. Standing-order tiles moved from their slightly smaller variant (68px, 15.5px label) to the standard tile. PaymentFlow lint warnings are unchanged (32 before and after). |
| 2026-09-24 | **Account holder name fixed; branch picker shared** | *Share account details* showed "Personal Banking" as the account holder. The retail profile's name is the relationship, not a person, and the cheque-book "Name printed" row had the same bug. New `accountHolderName()` in `src/lib/account-holder.ts`: joint accounts → every holder; business → the business name; retail → the signed-in customer. Both places now use it. `BranchCombobox` (type-to-search, arrow keys and Enter, clear button, opens upward when space is tight) moved out of `RequestCardFlow.tsx` into `src/components/ui/branch-combobox.tsx`. Card pickup and every branch field in Place a request use it, over `GCB_BRANCHES`; the request flow's own branch list is gone. |
| 2026-09-24 | **Add money only uses sources already linked** | Removed the dashed *New MoMo* / *New Card* buttons from *From linked wallet or card* in `LinkSourceAccountModal`. Wallets and cards are linked only from the Accounts page ("Link a card or wallet", `mode="link"`). With no linked sources, a note offers "Link one on Accounts" (`/accounts?link_source=true`). In fund mode the in-modal link screens and the card-return resume path can no longer be reached. They stay because link mode uses the same screens. |
| 2026-09-24 | **Accounts page — reviewed mockup built with 5 changes** | Took the mockup's layout (two titled sections, Default badge, rows with chips). The review changed five things. (1) The two yellow "Add Account" / "Add Source of Funds" buttons became quiet header pills in the Send & Pay "Standing Orders" style, so nothing outranks the list. (2) "Add Account" → **Open an Account** ("Open a GCB Account" for wallet customers); it still hands off to the opening portal. (3) "Source of Funds" (a compliance term) → **Linked Wallets & Cards**, and the action is **Link a Wallet or Card**. (4) Account rows lost their ⋯ menu: the whole row opens the account (chevron). **Make Default** and **Reactivate** (dormant accounts) moved to the Account Details header as pills, and a **Default** badge shows there too. Linked wallets and cards keep ⋯ → Remove. (5) Rows use the shared `TileChip` (exported from `action-tile.tsx`) on the tile surface (`--tile` / `--tile-border`), so they match `ActionTile`. The bottom "Open a new account" and "Link a card or wallet" rows are gone because the header pills replace them; the empty state keeps its own button. The header "Add money" button now shows for wallet-only customers only; GCB customers use Top up on each account. New `useCustomerAccounts()` (`src/lib/use-customer-accounts.ts`) gives the accounts list and the default for both pages. Catalog rows added. |
| 2026-09-24 | **Account Details — "Set as Default" toggle; tiles reordered** | Tile order: My Spends → Last 10 Transactions → Standing Orders → Place a Request → **Set as Default** (new `ToggleTile`, `src/components/ui/toggle-tile.tsx`: the `ActionTile` layout with the payment-success "Save as beneficiary" switch in place of the chevron). It shows only when there's more than one account and this one isn't dormant. Switching it on sets the default and shows a toast. When it's already on, it's locked, because there is always a default; the description says to set another account instead. The header "Make Default" pill and "Default" badge are gone because the toggle carries both. Reactivate stays in the header for dormant accounts. |
| 2026-09-24 | **Default shown on the balance card; tile is only the action** | The default account shows a yellow **Default** badge in the top-right corner of its balance card. The "Set as Default" tile (plain switch, no icon, no description) appears only on active accounts that aren't the default, when there's more than one account. Tapping it sets the default and shows the toast "{account} is now your default". `ToggleTile`: `icon` is optional, with a min-height of 72.5px so a chipless tile lines up with `ActionTile`s. `lockedBadge` is kept but unused on this page. |
| 2026-09-24 | **Add Account replaces "Open an Account"; Default badges are amber** | Accounts aren't opened from `/accounts` any more: the portal hand-off dialog and `OPENING_PORTAL` are gone, and the header pill is **Add Account** for every retail customer. New `src/components/accounts/AddAccountDialog.tsx` has 4 steps. **Verify It's You:** says up front that a selfie is coming (Cancel / Continue). **Take a Selfie:** the shared `SelfieCapture`, matched after 900ms; there's no mismatch path yet. **Choose an Account:** accounts held under the Ghana Card, where ones already on the profile show "Added" and can't be picked; single select, then *Add Account*. **Account Added:** "{name} is added" with type · number, then *Done*. There's no PIN because adding shows an account and moves no money. The data comes from the new `GHANA_CARD_ACCOUNT_IDS` in `mock-data.ts`, which adds two scenario-only accounts: *Home Project Savings* and *Salary Current Account*. They're saved in `useAccountPrefs.addedAccountIds` (`addAccount`, `clearAddedAccounts`, and changing the Dev Mode customer clears them), and `useCustomerAccounts()` merges them in, so they show on `/accounts` and resolve on Account Details. The Default badge on the Accounts list is now amber (`Badge` default), matching the balance card. **Open:** what should Add Account mean for a wallet-only customer, e.g. whether the wallet balance moves to the added account? The Dev Mode "migrated" scenario still demonstrates that banner. |
| 2026-09-24 | **No balances on the Cards list; Accounts lists match its white card** | `/cards` rows no longer show a balance, neither the card's own nor the linked account's. Balances stay on the card's detail page. The *Issue New Card* dialog keeps the available balance in its account picker, where it helps choose the funding account. The *Your Accounts* and *Linked Wallets & Cards* lists on `/accounts` now use the Cards list container (`rounded-2xl border-border bg-card`, `divide-border`, `hover:bg-muted/40`) instead of the grey tile surface. `TileChip` gained `tone="onCard"` (muted chip) for white lists; the white `onTile` chip stays the default for `ActionTile`/`ToggleTile`. |
| 2026-09-24 | **Add Account for wallet/card onboarders; ghost section buttons** | Each Dev Mode customer now carries `ghanaCardAccountIds`. *Wallet · MoMo* holds one GCB account (Salary Current) and *Wallet · card* holds none. `AddAccountDialog` takes `ghanaCardAccountIds` plus an optional `wallet` ({ balance, sourceTitles }). **No accounts under the Ghana Card:** a "No Accounts Found" empty state ("You can open one at any GCB branch…") with **Find a Branch** → `/locate-us`. **Wallet customers:** after picking an account there's an extra step, *Your Wallet Balance Moves*. It shows the wallet → account move with the amount, then three lines: the balance moves (no fees); {sources} top up {account} from now on; {account} becomes the default and the wallet leaves Accounts. The button reads **Add Account and Move Balance**, and success says "{amount} moved from your wallet". It's saved as `useAccountPrefs.walletMigration` ({ toAccountId, amount, dismissed }) and also sets the default. `useCustomerAccounts()` then drops the wallet, adds the amount to that account's balance with `sumMoney`, sets `isWalletCustomer` to false, and exposes `walletMigration`. Account Details reads the customer's copy of the account, so the balance matches. The "Your GCB account is ready" banner shows for a real move (dismissable without undoing it) or the Dev Mode *migrated* scenario. The GCB customer flow is unchanged. Section actions are now the system `Button variant="ghost" size="sm"`. |
| 2026-09-24 | **No Add money on `/accounts`** | The header "Add money" button (it was wallet-only) is gone, and the page header is just the title. Every account, including the GCB Wallet, tops up from *Top up* on its Account Details. The fund-mode `LinkSourceAccountModal`, `addMoneyOpen`/`fundResume` and the card-return resume path are removed from this page. The card return here now only confirms a link or offers *Try again* for Link a Wallet or Card. |
| 2026-09-24 | **Selfie retry/branch, My Spends motion + "Other" breakdown, and small account fixes** | **Add Account selfie miss:** "We Couldn't Match Your Selfie" (likely causes, **Try Again**, tries left). After 3 misses comes "Let's Finish This at a Branch" (Close / **Find a Branch** → `/locate-us`). The lock survives closing the dialog and resets when the Dev Mode customer changes. Dev Mode on `/accounts` has a new "Add Account selfie" group (Matches Ghana Card / Doesn't match). **My Spends motion:** the arc geometry and tween moved from `SpendsRadialChart` into the shared `src/components/charts/arc-tween.ts` (`createAnnularWedgePath`, `useTweenedArcs`, 420ms ease-in-out-cubic, snaps under reduced motion). The donut draws tweened wedges (clockwise from 12 o'clock, 2° gaps; a single segment is a full ring). `accountExpenses()` now uses `stableSlices()`: the 12-month window fixes each slot's category, order and colour, and zero slots stay so they can shrink and grow. **"Other" breakdown:** `CategorySlice.breakdown` gives each folded category's amount and share. Tapping *Other* expands an indented list under it, with a chevron. The heading "Category breakdown" is now 16px medium foreground (like "Sources of Funds"); rows went to 15px name, 13px %, 15px amount. **Accounts:** the header is now "Sources of Funds" at Ransford's request. *Remove* opens a warning dialog ("Remove {source}?" plus what stops working, Cancel / destructive Remove), and Undo is kept. **Account Details:** "Set as Default" is a plain row (`ToggleTile variant="row"`, no card). The Last 10 dialog footer is now **View All Transactions** → `/transactions?account=<id>`; `TransactionList` takes `initialAccountId` and the page reads `?account=`. |
| 2026-09-24 | **Fix: Back loop between Place a Request and Account Details** | `RequestFlow` left the flow with `router.push("/accounts/[id]")`. That pushed the parent on top of the child, so Account Details' Back (`router.back()`) returned to the request page and the two looped. Leaving the flow (Back on the chooser or on a `?type=` deep link, and *Back to account* on the receipt) now uses `useContextualBack(backToAccount)`: it pops when there's in-app history and otherwise replaces. This follows the existing "Back navigation (no loops)" rule in LEARNINGS.md. |
| 2026-09-24 | i18n pass on new screens (Accounts, Add Account, Requests, Expenses, Add money / link modal, card verification): ~140 FR/ES/ZH catalog rows; split sentences merged into single text runs | New screens were rendering English under FR/ES/ZH; coverage script is the source of truth for gaps |
| 2026-09-24 | **Place a Request — free emailed statement; cheque book delivery and quantity** | **Statement:** it's free and email-only. Period (or custom dates), then **Send to**, an email field prefilled with the registered address and editable. **Send Statement** skips review and PIN, since there's nothing to charge, and goes straight to "Statement Sent". There's no branch option and no fee row. The "Download a statement now" link is gone because it pulled people out of the flow. **Cheque book:** booklet size (fee shown "each"), then **Number of booklets** (new `Stepper`, 1–5), then **Mode of delivery** → review (fee = size fee × count via `multiplyMoney`) → PIN. The delivery section moved out of `RequestCardFlow` into the shared `src/components/ui/delivery-mode-fields.tsx` (`DeliveryModeFields`, `deliveryComplete`, `deliverySummary`). It has branch pickup with `BranchCombobox`, or doorstep delivery with recipient name, address, city and phone. Request a Card uses it unchanged. The doorstep recipient defaults to the account holder. Ready: pickup "In 3 working days", delivery "In 3–5 working days". **Letter:** unchanged; its fees now live in `LETTER_DELIVERY`. |
| 2026-09-25 | **Onboarding copy, referral step, onboarding link pop-up** | On both `/signup` (new to GCB) and `/activate` (existing customers), the step titles now read "Take a Selfie" → **Selfie Match**, "Enter Verification Code" → **Confirm Your Code** and "Create Password" → **Create Your Password**. **New optional referral step** after the password and before the PIN: "Welcome, {first name}" and "Reward the person who referred you by entering their referral code. This step is optional." It offers **Enter Referral Code** (reveals a code field and **Apply Code**) or **Skip & Proceed**, both equal weight so it never gates sign-up. It's the shared `src/components/auth/ReferralStep.tsx`: 6–10 letters or digits, and `000000` shows the not-found error. Step progress went from 9 to 10, and Back goes PIN → referral → password. The four guided tours that cross the password step now include a `*-referral` stop. **After new-to-GCB sign-up**, `/accounts?link_source=true` opens the link pop-up with `onboarding` set. The title is **Link Source Account**, with "Choose an account or payment method to link to your new account." and the mobile app's tiles: grey tile, yellow round icon, title, two-line hint, dark chevron (*Link Mobile Money Wallet* / *Link a Card*). Opening it from the "Link a Wallet or Card" pill keeps the regular version. Also: the Dev Mode dock (`PersonaFlowSwitcher`) renders after mount, fixing a hydration mismatch. Referral codes are branch codes: the referrer (branch) name must show before *Apply Code*, e.g. 183 → Tema Community (Meridian). The OTP step is titled **Confirm Your Code**. The new-to-GCB password checklist uses the amber tick. Catalog rows are in `onboarding.ts`. |
---

## Next Steps

1. **Implement Remaining Customer Flow Screens**: Complete the Transfer Between Accounts flow and FX Conversion interface.
2. **Implement Admin Maker-Checker Workflow**: Wire up `ComplianceActionDialog` and role-gated approval triggers.
3. **Verify All 13.1 States Across Routes**: Test `ListSkeleton`, `TrueEmptyState`, `FilteredEmptyState`, `ListErrorState`, and `PartialLoadFooter` on all data tables and lists.
4. **Answer the remaining decisions D2, D3, D5** in [`DESIGN-TIMELINE.md`](../DESIGN-TIMELINE.md) §4 before Figma Week 1 (Mon 31 Aug 2026). D1 and D4 are settled.
5. **Implement D1 in the prototype** — pre-login Personal/Business chooser, two sign-in doors, remove the post-login profile switcher, and split `src/lib/navigation.ts` into a Personal model (hubs 1–7) and a Business model (Trade / Approvals / Administration).
6. **Build out the remaining Send Money rails** — proxy, group and PAPSS have real step sets but representative logic only (proxy resolves against a fixture directory, group has no per-recipient validation, PAPSS rates are fixtures).
7. **Onboarding Pass 2** — (a) split "Wallet or Card" into two distinct `/get-started` options + `/signup?flow=wallet|card` framing; (b) add a device-trust selfie step to the new-device `/mfa` path; (c) add a **primary-account picker** to `/activate` when the matched Ghana Card maps to >1 eligible account (choose the internet-banking primary before finishing). Then extend/adjust the tour steps in `src/lib/tours.ts` to cover the new screens.
8. **Accounts model follow-ups** — apply Option B (default-account balance + switcher, no sources in totals) to the dashboard hero; add catalog rows for the new `/accounts` copy; wire the "on its way" pending state into Add money once backend confirms the auto-sweep; decide whether Business profiles can link sources.
9. **i18n follow-ups** — native-speaker review of FR/ES/ZH banking + legal copy; add the language toggle to the admin shell header (language already carries over via localStorage); add catalog rows whenever `npx tsx scripts/i18n-coverage.mts` shows new UI copy.
